---
interface Props {
  date: string;
  description: string;
}

const { date, description } = Astro.props;
---

<a href="#join-us" class="fixed top-4 left-4 z-50">
  <div
    class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-lg p-2 flex items-center max-w-max"
  >
    <div class="">
      <span class="text-3xl">🗓️</span>
    </div>
    <div class="border-l border-gray-300 ml-1 px-2">
      <p class="font-bold text-gray-900 text-sm">{date}</p>
      <p class="text-gray-600 text-xs">{description}</p>
    </div>
  </div>
</a>
